extends@: parent@

form:
  fields:
    tabs:
      fields:
        content:
          fields:

            header.onpage_menu:
              type: toggle
              toggleable: true
              label: On-page Menu
              help: "If disabled, uses the normal page menu. Otherwise, lists the on-page sections"
              highlight: 1
              options:
                1: PLUGIN_ADMIN.ENABLED
                0: PLUGIN_ADMIN.DISABLED